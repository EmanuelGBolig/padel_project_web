{
  "name": "theme",
  "version": "1.0.0",
  "description": "Tailwind compilado para TodoPadel (reemplaza el CDN).",
  "scripts": {
    "build": "tailwindcss -i ./src/styles.css -o ../static/css/tailwind.css --minify"
  },
  "license": "MIT",
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
